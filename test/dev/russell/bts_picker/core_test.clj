(ns dev.russell.bts-picker.core-test
  (:require [clojure.test :refer :all]
            [dev.russell.bts-picker.core :as impl]))

(deftest a-test
  (testing "FIXME."
    (is (not (nil? (impl/-main))))))
