(ns dev.russell.bts-beater.core-test
  (:require [clojure.test :refer :all]
            [dev.russell.bts-beater.core :as impl]))

(deftest a-test
  (testing "FIXME."
    (is (not (nil? (impl/-main))))))
